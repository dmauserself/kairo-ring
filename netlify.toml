# Netlify detects Next.js and adds its runtime automatically
# (the /checkout page is rendered on demand by a Netlify Function).
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
